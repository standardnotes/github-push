var Octo = require('./')
var octo = new Octo({token: process.env['GH_TOKEN']})

octo.repos('philschatz', 'octokat.js').issues(1).fetch()
// octo.user.emails.fetch()
.then((res) => {
  console.log('-------------- PROMISE RESPONSE PHIL-------')
  // console.log(res)
  console.log(res)
  // return res.items[0].columns.fetch()
  // .then((cols) => {
  //   // console.log(cols)
  //   return cols.items[0].cards.fetch()
  //   .then((cards) => {
  //     console.log(cards)
  //     return cards.items[0].fetch()
  //     .then((card) => {
  //       console.log(card)
  //     })
  //   })
  // })

  // return res.nextPage.fetch()
  // .then((two) => {
  //   console.log('-----------------------')
  //   // console.log(two)
  // })
})
.catch((err) => {
  console.log('-------------- PROMISE REJECTION PHIL-------')
  console.log(err)
})
